<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head') %>
    <body class="container-fluid">
        <%- include('../partials/header') %>
        <main class="container">
            <div class="row d-flex flex-column justify-content-start align-items-center mt-4">
                <h1> Edit <span class="text-dark">Your Memory</span></h1> <br />
                <h4>Choose to edit or delete your memory</h4>
            </div>
            <div class="row d-flex flex-column justify-content-center align-items-center">
                <div class="col-md-6">
                    <form class="" action="/memories/<%= memory._id %>?_method=PUT" method="POST">
                        <div class="form-group">
                            <label for="title" class="text-white">Title</label>
                            <input type="text" class="form-control" name="title" value="<%= memory.title %>" required>
                        </div>
                        <div class="form-group">
                            <label for="caption" class="text-white">Caption</label>
                            <textarea class="form-control" name="caption" required><%= memory.caption %></textarea>
                        </div>
                        <div class="form-group">
                            <a href="/memories/<%= memory._id %>" class="btn btn-danger">Cancel</a>
                            <button type="submit" class="btn btn-large btn-primary">SAVE MEMORY</button>
                            <a class="btn btn-danger" id="delete-btn">Delete Memory</a>
                        </div>
                    </form>
                </div>
            </div>
            
        </main>
        <%- include('../partials/footer') %>
        <div id="modal" class="modal container-fluid">
            <div class="container">
                <div class="row d-flex flex-column justify-content-center align-items-center">
                    <div class="col-md-8 col-12 align-items-center mt-4">
                        <div class="row d-flex flex-column justify-content-center align-items-center">
                            <p>Are you sure you want to delete this memory?</p>
                        </div>
                        <div class="row d-flex flex-column justify-content-center align-items-center">
                            <a href="/memories/<%= memory._id %>" class="btn btn-danger" rel="modal:close">NO</a>
                            <form action="/memories/<%= memory._id %>?_method=DELETE" method="POST">
                                <button class="btn btn-large" type="submit">YES</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>